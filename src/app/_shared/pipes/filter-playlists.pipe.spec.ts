import { FilterPlaylistsPipe } from './filter-playlists.pipe';

describe('FilterPlaylistLocationPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPlaylistsPipe();
    expect(pipe).toBeTruthy();
  });
});
