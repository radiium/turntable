import { FilterPlaylistLocationPipe } from './filter-playlist-location.pipe';

describe('FilterPlaylistLocationPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPlaylistLocationPipe();
    expect(pipe).toBeTruthy();
  });
});
